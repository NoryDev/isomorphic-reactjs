<%= react_component('PostsList',
                    { initialPosts: @posts },
                    { prerender: params[:noprerender].nil? })
                    #will allow the deactivation of pre-rendering using http://localhost:3000?noprerender=1
                    %>
